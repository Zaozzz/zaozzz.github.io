/* ============================================================
   Xu Wang Blog · Blog
   Foundation tokens — colors, type, spacing, radii, shadow
   ============================================================ */

/* ---- System fonts (no external CDN needed) ------------------
   Editorial direction: a high-contrast serif for display and
   pull-quotes; a quietly modern sans for UI chrome; a humanist
   serif for long-form body text; mono for code.
---------------------------------------------------------------*/

:root {
  /* ============================================================
     PAPER & INK — the spine of the system
     ============================================================ */
  --paper:          #F5F1E8;   /* warm ivory page */
  --paper-soft:     #EDE7D7;   /* tone for cards/sections */
  --paper-deep:     #E4DCC6;   /* recessed surfaces */
  --paper-shadow:   #D6CCB2;   /* sectional bands */

  --ink:            #1A1612;   /* primary text — deep warm black */
  --ink-soft:       #3A332B;   /* secondary text */
  --ink-mute:       #6B6258;   /* tertiary, captions, meta */
  --ink-faint:      #968B7B;   /* hints, disabled */
  --hairline:       #D9D0BC;   /* 1px rules, dividers */
  --hairline-soft:  #E6DFCC;

  /* ============================================================
     ACCENT — "Dawn Rust". Used sparingly, almost as a seal.
     ============================================================ */
  --rust:           #A03A1F;   /* primary accent */
  --rust-deep:      #7A2A14;   /* hover/active */
  --rust-soft:      #C95A38;   /* tint */
  --rust-wash:      #EFD5C8;   /* fill, badges */
  --rust-paper:     #F5E2D6;   /* lightest tint background */

  /* Secondary "Pine" — research / academic green */
  --pine:           #3F5D3C;
  --pine-soft:      #6B8A5A;
  --pine-wash:      #D7DFCB;

  /* Secondary "Indigo" — for engineering category */
  --indigo:         #2A3656;
  --indigo-soft:    #4F628E;
  --indigo-wash:    #C9D1E2;

  /* "Honey" — gentle gold; used by bulb gift and warm hovers */
  --gold:           #C28A2B;
  --gold-soft:      #E6BA5C;
  --gold-wash:      #F2DEB1;

  /* "Petal" — soft pink for the flower gift */
  --petal:          #D88AA0;
  --petal-soft:     #E8B0C0;

  /* "Coffee" — warm brown for the cup gift */
  --coffee:         #7B5530;
  --coffee-dark:    #5C3F22;

  /* ============================================================
     HIGHLIGHTER — translucent marker, for in-text annotations
     ============================================================ */
  --hl-yellow:      rgba(245, 208, 97, 0.45);
  --hl-yellow-edge: rgba(217, 169, 30, 0.85);
  --hl-pink:        rgba(242, 165, 181, 0.50);
  --hl-pink-edge:   rgba(214, 105, 130, 0.85);
  --hl-mint:        rgba(156, 201, 184, 0.55);
  --hl-mint-edge:   rgba(95, 156, 130, 0.85);
  --hl-blue:        rgba(160, 198, 222, 0.55);
  --hl-blue-edge:   rgba(82, 138, 175, 0.85);

  /* ============================================================
     SEMANTIC
     ============================================================ */
  --success:        #4A6A2E;
  --warning:        #B5732A;
  --danger:         #A03A1F;
  --info:           #2A4F6E;

  /* Category seals */
  --cat-eng:        var(--indigo);
  --cat-eng-wash:   var(--indigo-wash);
  --cat-research:   var(--pine);
  --cat-research-wash: var(--pine-wash);
  --cat-life:       var(--rust);
  --cat-life-wash:  var(--rust-wash);

  /* ============================================================
     TYPE FAMILIES
     ============================================================ */
  --font-display:   Georgia, "Songti SC", "STSong", "SimSun", serif;
  --font-serif:     Georgia, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", -apple-system, sans-serif;
  --font-sans:      -apple-system, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", sans-serif;
  --font-mono:      "SF Mono", Menlo, Consolas, "Courier New", monospace;
  --font-cn-display:"Songti SC", "STSong", "SimSun", serif;

  /* ============================================================
     TYPE SCALE — editorial, generous
     Base 16px. Display is high contrast italic-capable.
     ============================================================ */
  --t-display-xl: 88px;   /* hero numerals / curtain */
  --t-display-l:  64px;   /* article title */
  --t-display-m:  44px;
  --t-display-s:  32px;
  --t-h1:         28px;
  --t-h2:         22px;
  --t-h3:         18px;
  --t-body:       19px;   /* long-form sans-serif body — bumped for readability */
  --t-ui:         15px;   /* UI sans */
  --t-small:      13px;
  --t-meta:       12px;
  --t-micro:      11px;

  --lh-tight:     1.08;
  --lh-snug:      1.25;
  --lh-body:      1.62;   /* generous, editorial */
  --lh-loose:     1.85;

  --ls-display:  -0.012em;
  --ls-tight:    -0.01em;
  --ls-normal:    0;
  --ls-wide:      0.04em;
  --ls-eyebrow:   0.18em;   /* small-caps eyebrows */

  /* ============================================================
     SPACING — 4px ramp
     ============================================================ */
  --sp-0:   0;
  --sp-1:   4px;
  --sp-2:   8px;
  --sp-3:   12px;
  --sp-4:   16px;
  --sp-5:   20px;
  --sp-6:   24px;
  --sp-8:   32px;
  --sp-10:  40px;
  --sp-12:  48px;
  --sp-16:  64px;
  --sp-20:  80px;
  --sp-24:  96px;
  --sp-32:  128px;

  /* Editorial measure — column widths */
  --measure-article: 68ch;
  --measure-aside:   42ch;
  --measure-wide:    92ch;

  /* ============================================================
     RADII — restrained. Editorial corners stay sharp.
     ============================================================ */
  --r-0: 0;
  --r-1: 2px;
  --r-2: 4px;
  --r-3: 8px;
  --r-4: 12px;
  --r-pill: 999px;

  /* ============================================================
     SHADOW — soft, paper-on-paper
     ============================================================ */
  --shadow-1: 0 1px 0 rgba(26,22,18,0.04);
  --shadow-2: 0 2px 8px -2px rgba(26,22,18,0.08), 0 1px 2px rgba(26,22,18,0.04);
  --shadow-3: 0 12px 32px -12px rgba(26,22,18,0.18), 0 2px 6px rgba(26,22,18,0.06);
  --shadow-4: 0 24px 60px -24px rgba(26,22,18,0.28), 0 4px 10px rgba(26,22,18,0.08);
  --shadow-inset: inset 0 0 0 1px var(--hairline);

  /* Protection gradient — for hero edges */
  --grad-paper-fade: linear-gradient(180deg, rgba(245,241,232,0) 0%, rgba(245,241,232,0.85) 70%, var(--paper) 100%);
  --grad-sidebar:    linear-gradient(180deg, var(--paper) 0%, var(--paper-soft) 100%);

  /* ============================================================
     MOTION — restrained, paper-like
     ============================================================ */
  --ease-out:    cubic-bezier(0.22, 0.61, 0.36, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);

  --dur-instant: 90ms;
  --dur-fast:    180ms;
  --dur-base:    280ms;
  --dur-slow:    480ms;
  --dur-curtain: 720ms;
}

/* ============================================================
   SEMANTIC ELEMENTS — defaults for prose pages
   ============================================================ */
body {
  font-family: var(--font-sans);
  font-size: var(--t-ui);
  line-height: var(--lh-snug);
  color: var(--ink);
  background: var(--paper);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

.prose, .prose p, .prose li {
  font-family: var(--font-serif);
  font-size: var(--t-body);
  line-height: var(--lh-body);
  color: var(--ink);
  text-wrap: pretty;
}

h1, .h1 {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: var(--t-display-l);
  line-height: var(--lh-tight);
  letter-spacing: var(--ls-display);
  color: var(--ink);
}
h2, .h2 {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: var(--t-display-s);
  line-height: var(--lh-snug);
  letter-spacing: var(--ls-tight);
}
h3, .h3 {
  font-family: var(--font-display);
  font-weight: 500;
  font-size: var(--t-h1);
  line-height: var(--lh-snug);
}
h4 {
  font-family: var(--font-sans);
  font-weight: 600;
  font-size: var(--t-h2);
  letter-spacing: var(--ls-tight);
}

.eyebrow {
  font-family: var(--font-sans);
  font-weight: 600;
  font-size: var(--t-meta);
  letter-spacing: var(--ls-eyebrow);
  text-transform: uppercase;
  color: var(--ink-mute);
}

.meta {
  font-family: var(--font-sans);
  font-size: var(--t-small);
  color: var(--ink-mute);
}

code, kbd, samp {
  font-family: var(--font-mono);
  font-size: 0.92em;
}

blockquote {
  font-family: var(--font-display);
  font-style: italic;
  font-size: 26px;
  line-height: 1.45;
  color: var(--ink-soft);
  border-left: 2px solid var(--rust);
  padding-left: var(--sp-6);
  margin: var(--sp-8) 0;
}

a {
  color: var(--ink);
  text-decoration: underline;
  text-decoration-color: var(--rust);
  text-underline-offset: 3px;
  text-decoration-thickness: 1px;
  transition: text-decoration-thickness var(--dur-fast) var(--ease-out),
              color var(--dur-fast) var(--ease-out);
}
a:hover {
  text-decoration-thickness: 2px;
  color: var(--rust-deep);
}

hr {
  border: 0;
  height: 1px;
  background: var(--hairline);
  margin: var(--sp-10) 0;
}

::selection {
  background: var(--hl-yellow);
  color: var(--ink);
}

/* Highlighter classes — applied by the reader */
mark.hl-yellow { background: var(--hl-yellow); padding: 0 .15em; box-decoration-break: clone; }
mark.hl-pink   { background: var(--hl-pink);   padding: 0 .15em; box-decoration-break: clone; }
mark.hl-mint   { background: var(--hl-mint);   padding: 0 .15em; box-decoration-break: clone; }
mark.hl-blue   { background: var(--hl-blue);   padding: 0 .15em; box-decoration-break: clone; }
